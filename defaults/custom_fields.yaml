---
custom_field_choices:
  - custom_field: phpipam
    display: 'discoverSubnet'
    value: discoverSubnet
  - custom_field: phpipam
    display: 'pingSubnet'
    value: pingSubnet
  - custom_field: phpipam
    display: 'resolveDNS'
    value: resolveDNS
  - custom_field: phpipam
    display: 'showName'
    value: showName

  - custom_field: snmp_credentials
    display: cred-1
    value: cred-1
  - custom_field: snmp_credentials
    display: cred-2
    value: cred-2

custom_fields:
  - label: phpipam
    description: PHPIPAM Settings
    type: select
    required: false
    weight: 100
    content_types: [ ipam.prefix ]

  - label: snmp_credentials
    description: SNMP Credentials
    type: select
    required: false
    weight: 100
    content_types: [ dcim.device ]

  - label: net
    description: Name of net
    type: text
    required: false
    weight: 100
    content_types: [ dcim.device ]

  - label: scan_prefix
    description: Scan Prefix
    type: boolean
    required: false
    weight: 100
    content_types: [ ipam.prefix ]
